# ─── Production ───────────────────────────────────────
# VITE_ variables are baked into the JS bundle at build time.
# Change these to match your production server before running `make dist-web`.

VITE_API_BASE_URL=http://172.19.185.85:8080/api/v1
VITE_REFRESH_ENDPOINT=/auth/refresh
VITE_GUEST_USERNAME=guest
VITE_GUEST_PASSWORD=guest123
